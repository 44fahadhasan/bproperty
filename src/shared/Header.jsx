import Navbar from "../components/Header/Navbar";

const Header = () => {
  return (
    <header class="open-sans flex flex-wrap md:justify-start lg:flex-nowrap z-50 w-full bg-white">
      {/* navbar */}
      <Navbar />
    </header>
  );
};

export default Header;
